<h1><%= @patient.name %></h1>

<h2><%= @patient.age %></h2>

<% @patient.appointments.each do |app| %>
    <p>Time: <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p> ###
    <p>Doctor: <%= link_to app.doctor.name, doctor_path(app.doctor) %> ###
<% end %>